package com.mrcha.gymlogger.ui

import androidx.compose.ui.graphics.Color

/**
 * Dark-fantasy visual tokens — a warrior's training ledger: obsidian ground,
 * worn-steel hairlines, blood only where it means something (PR, pain flag,
 * primary action).
 */
object Forge {
    val Ground = Color(0xFF08080A)
    val Panel = Color(0xFF101114)
    val PanelHi = Color(0xFF15161A)
    val Hairline = Color(0xFF1E2025)
    val Steel = Color(0xFF3A3D43)

    val Parchment = Color(0xFFEDE7DA)
    val Bone = Color(0xFFCBBFA6)
    val Ash = Color(0xFF8C9098)
    val Slate = Color(0xFF5E6167)

    val Blood = Color(0xFF8E1414)
    val BloodBright = Color(0xFFC0392B)
    val Ember = Color(0xFFE4573F)

    /** Muscle fills, drained (no volume yet) and saturated (worked hard). */
    val MuscleDrained = Color(0xFF191416)
    val MuscleLow = Color(0xFF2A2224)
    val MuscleMid = Color(0xFF3A2628)
    val MuscleWarm = Color(0xFF7A2426)
    val MuscleHot = Color(0xFFC0392B)
    val MuscleStrong = Color(0xFF96262A)
    val MuscleOutline = Color(0xFF4A4045)
    val MuscleOutlineDim = Color(0xFF332B2D)
}
