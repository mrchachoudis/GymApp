<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose supplies the real theme; this only needs to exist so the
         activity has something to start with before the first frame. -->
    <style name="Theme.GymLogger" parent="android:Theme.Material.NoActionBar" />
</resources>
